import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "I Got You - Discover Hidden Outdoor Gems",
  description: "Find quiet, lesser-known outdoor destinations with fewer than 300 reviews but amazing experiences. AI-powered hidden gems discovery.",
  keywords: ["travel", "hidden gems", "outdoor", "destinations", "AI", "adventure"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
